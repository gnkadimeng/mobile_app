# Refuse to install on a Node version outside package.json "engines".
# This is what stops a contributor on a different Node from committing a
# package-lock.json that CI (Node 20) then rejects with `npm ci`.
engine-strict=true
